# Case 1: Anonymous — no auth, no TLS
# Run from repo root: mosquitto -c telegrapher/test/1_anonymous.conf
# systemconfig.json traits: { "broker": "tcp://localhost:7001" }
# (no "security" block needed)

listener 7001
allow_anonymous true
